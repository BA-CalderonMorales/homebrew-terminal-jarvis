name: Security Scan

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  security:
    uses: BA-CalderonMorales/.github/.github/workflows/security-scan.yml@develop
